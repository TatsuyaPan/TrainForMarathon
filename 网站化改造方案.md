# TrainForMarathon 核心优先改造方案

> 版本：v2.1  
> 日期：2026-09-10  
> 状态：方向已确认，待实施计划  
> 首版定位：先完成平台无关的马拉松课程与动态课表核心，再以微信小程序作为第一个产品外壳。

> 实施状态：核心库首个可运行版本和数据访问边界已建立；微信小程序工程壳与微信云开发适配入口已建立，页面功能尚待实现。

---

## 0. 结论摘要

首版不做网站，不追求多端同时上线，也不提前建设分享、社区、复杂同步等外围能力。

采用三层架构：

1. **核心库**：课程内容、训练类型、配速计算、动态课表、进度与统计计算；
2. **数据访问层**：用统一接口包装用户配置、课表和进度读写；
3. **微信小程序**：微信登录、页面展示、用户操作，并提供微信云开发适配器。

核心库不依赖微信、腾讯云、浏览器或任何 UI 框架。核心库与小程序位于独立目录，小程序单向引用核心库和数据访问层，不在页面代码中重复训练规则或直接读写云数据库。以后增加网站、其他小程序或 App 时，都继续复用该核心。

| 决策项 | 首版结论 |
|---|---|
| 主产品 | 微信原生小程序 |
| 网站 | 不做，移出首版范围 |
| 核心 | 纯 TypeScript 包，平台无关 |
| 内容 | Markdown 原文 + 构建后的平台无关内容数据 |
| 课表 | 结构化模板 + 参数化实例 |
| 配速 | 核心库统一计算 |
| 登录与存储 | 微信云开发身份 + 最小数据集合，经数据访问层调用 |
| 统计 | 核心库计算，小程序展示 |
| 首版原则 | 先完成可验证的核心，再从核心向外生长 |

---

## 1. 产品目标与范围

### 1.1 首版目标

用户能够在微信小程序中完成以下闭环：

1. 自动获得微信身份并进入产品；
2. 阅读现有马拉松课程内容；
3. 输入参考成绩，得到训练配速；
4. 选择 20 周或五周循环课表；
5. 设置比赛日期、能力和最大周跑量，得到参数化课表；
6. 查看当前周和当天训练；
7. 标记训练完成情况；
8. 查看基础训练统计。

### 1.2 首版明确不做

- Astro 网站或其他 Web 前端；
- H5 内嵌小程序；
- 社区、评论、私信或排行榜；
- 分享图片和复杂传播功能；
- AI 教练或自由生成训练内容；
- 手表及第三方运动平台接入；
- 多设备实时协作和复杂冲突解决；
- 自动为每一天分配完整跑量；
- 同时维护多个后端实现（首版只实现微信云开发适配器）。

### 1.3 后续生长方式

首版完成后，新能力按独立外层增加：

- 网站：调用同一个核心库渲染内容、计算配速和生成课表；
- 分享：消费核心库输出的分享数据模型；
- 训练日志：扩展核心记录模型和持久化集合；
- 更完整的课表生成：补充经过确认的跑量分配规则；
- 其他客户端：重新实现 UI 与存储适配，不复制业务规则。

---

## 2. 架构设计

```text
┌────────────────────────────────────────────────────┐
│ packages/core                                      │
│                                                    │
│ content   课程索引、章节、训练类型                   │
│ pace      VDOT/配速计算                            │
│ plans     课表模板、日期绑定、实例化                 │
│ progress  完成记录模型                             │
│ stats     完成次数、距离、完成率等统计               │
│ schema    类型、校验、版本迁移                       │
│                                                    │
│ 纯 TypeScript；无 wx、CloudBase、DOM、UI 和网络依赖  │
└────────────────────────┬───────────────────────────┘
                         │ 标准函数与可序列化数据
┌────────────────────────▼───────────────────────────┐
│ 私有仓库 train-for-marathon-wapp                   │
│                                                    │
│ core/  Git submodule → 当前核心与内容仓库           │
│ wapp/  原生小程序 / 数据服务 / 微信云开发适配器      │
└────────────────────────┬───────────────────────────┘
                         │
┌────────────────────────▼───────────────────────────┐
│ 微信云开发                                          │
│ 微信身份 / 用户设置 / 课表实例 / 完成记录            │
└────────────────────────────────────────────────────┘
```

### 2.1 依赖规则

| 模块 | 可以依赖 | 禁止依赖 |
|---|---|---|
| `packages/core` | TypeScript、必要的纯校验库 | 微信 API、CloudBase SDK、DOM、Canvas、UI 框架 |
| `train-for-marathon-wapp/wapp/src/data-access` | `core` 类型、抽象数据接口 | 页面和训练规则 |
| `train-for-marathon-wapp/wapp` | submodule 中的 `core`、数据访问层、微信 API | 自行实现配速或课表规则、页面直接读写数据库 |
| 可选云函数 | `core` 中可用于服务端的纯模块 | 小程序页面或组件 |

两个 Git 仓库物理隔离。依赖方向固定为 `wapp 页面 → wapp 数据访问层 → core submodule`，小程序也可直接调用核心的只读或纯计算 API。核心仓库不能反向引用 wapp。所有核心输入和输出必须可以序列化为 JSON。

### 2.2 核心库对外接口

核心库提供明确的领域 API，外部无需了解内部实现：

```ts
getContentIndex()
getContentById(contentId)
getTrainingType(typeId)

calculatePaces(referenceResult)

listPlanTemplates()
instantiatePlan(templateId, parameters)
validatePlanTemplate(template)

applyProgress(planInstance, progressRecords)
calculateStats(planInstance, progressRecords)
```

具体函数名可在实施计划中调整，但职责边界保持不变。

### 2.3 核心库不负责的内容

- 用户是谁以及如何登录；
- 数据保存在哪里；
- 页面如何展示；
- 网络失败如何重试；
- 微信授权、审核和隐私弹窗；
- 分享、支付、通知等平台能力。

这些都属于小程序或具体存储适配层。

---

## 3. 内容系统

### 3.1 内容资产

当前 `src/md` 下有 23 个 Markdown 文件，约 47 KB，继续作为课程正文的编辑源。构建过程将 Markdown 转换为平台无关的内容块或安全富文本数据，再随核心内容包提供给小程序。

小程序运行时不读取文件系统，也不直接解析任意 HTML。构建产物应包含：

- 稳定内容 ID；
- 标题、章节、排序和上下篇关系；
- 已处理的正文内容；
- 图片资源引用；
- 训练类型和课表的结构化引用；
- `contentVersion`。

### 3.2 单一事实源

| 内容 | 唯一事实源 |
|---|---|
| 训练原理、说明和注意事项 | Markdown |
| 课程目录与顺序 | 结构化内容清单 |
| 训练类型元数据 | 结构化数据 |
| 20 周和五周课表 | 结构化 PlanTemplate |
| 配速算法及测试样例 | 核心 TypeScript + 合法数据来源 |

课表不再以 Markdown 大表格作为长期事实源。迁移时将原表一次性转为结构化模板并逐格校对；以后由模板生成小程序界面以及需要的静态表格，避免两份内容漂移。

### 3.3 内容查询

小程序只能通过核心库的内容 API 查询课程。页面不得写死章节路径、训练类型说明或课表文本。这样未来调整内容目录时，只更新内容包，不修改所有页面。

---

## 4. 动态课表系统

### 4.1 双层模型

#### PlanTemplate

静态模板包含：

- 模板 ID、名称、版本；
- 周次、阶段和周跑量比例；
- 每天的训练类型、组合训练、可替换项与备注；
- `E / M / T / I / R / L / ST / REST / RACE` 等类型 ID；
- 原文已经明确的距离、时长、组数和恢复方式。

#### PlanInstance

实例由模板和用户参数生成，包含：

- `templateId` 与 `templateVersion`；
- 比赛日期和每周绝对日期；
- 参考成绩或 VDOT；
- 最大周跑量与各周跑量目标；
- 每天的训练类型与具体配速；
- 用户确认的距离或时长；
- 用户对训练日的移动或替换。

实例生成必须是确定性的：相同模板版本和相同参数得到相同结果。

### 4.2 首版“动态”的边界

首版动态课表支持：

- 根据比赛日期生成每周和每天的绝对日期；
- 根据能力值显示训练配速；
- 根据最大周跑量计算每周跑量目标；
- 将训练项移动到同一周的其他日期；
- 选择模板中已经定义的替换项；
- 定位当前周并叠加完成记录。

首版不自动推导所有每日距离。模板明确距离时直接显示；未明确的 E/L/M 项由用户确认计划距离，或者训练后只记录实际距离。未确认计划距离时仍可打卡，但统计中不计算“按距离完成率”。

### 4.3 模板校验

构建时必须校验：

- 20 周模板的周次从 20 连续到 1；
- 五周模板的周次从 5 连续到 1；
- 所有训练类型 ID 已注册；
- 比例、距离、时间和组数合法；
- 组合训练存在有效子项目；
- 模板引用的内容存在；
- 模板版本和 schema 版本兼容。

当前 20 周原文最后一行写成“第 8 周”，按上下文应为“第 1 周”，迁移前需要作者确认并修正。

---

## 5. 配速系统

配速系统属于核心库，不属于腾讯账号系统。用户成绩和当前配速设置经数据访问层保存在微信云开发中，但计算只由核心库执行。

| 项 | 首版规格 |
|---|---|
| 输入 | 5K、10K、半马或全马参考成绩 |
| 输出 | 能力值、E/M/T/I/R 配速、跑步机速度 |
| 使用位置 | 配速计算页、用户设置、动态课表 |
| 一致性 | 所有页面调用同一个核心 API，不保存重复计算规则 |
| 校验 | 成绩格式、合理范围和算法边界均有测试 |

VDOT 数据不能以“开源非盈利、标注来源”为理由直接复制整表。实施前必须取得授权、采用可合法实现的公开公式，或只实现已明确授权的估算规则。页面需标明计算依据、误差和非医疗建议属性。

---

## 6. 用户、记录与基础统计

### 6.1 登录

首版使用微信云开发的小程序身份。用户首次进入时获得稳定身份，无需额外创建用户名和密码。身份解析封装在微信适配器中，页面层和核心库都不接触微信密钥或管理端凭据。

### 6.2 最小数据模型

| 数据 | 用途 |
|---|---|
| UserSettings | 当前参考成绩/VDOT、最大周跑量、偏好设置 |
| UserPlan | 用户当前课表实例、模板版本和比赛日期 |
| ProgressRecord | 某日训练的状态、实际距离、实际时长和更新时间 |

首版不保存无关微信资料，不默认采集头像、昵称、手机号或位置。

### 6.3 完成记录

训练状态仅包含：未开始、已完成、部分完成、跳过。用户可选填实际距离和时长。写入时使用稳定记录 ID，重复提交不得产生重复记录。

### 6.4 基础统计

统计由核心库根据课表实例和完成记录计算，微信云开发不保存可重复推导的汇总值。首版展示：

- 本周计划训练次数与已完成次数；
- 本周完成率（按训练次数）；
- 本周实际跑量；
- 整个课表累计完成次数；
- 连续完成训练周数：截至最后产生训练记录的一周，向前连续完整完成的周数。

若计划距离未填写，不展示按距离完成率，避免产生虚假精度。

---

## 7. 微信小程序职责

小程序是首版唯一 UI，包含以下页面：

1. **首页**：当前课表、当前周、下一项训练和本周摘要；
2. **课程**：章节目录、文章阅读、训练类型详情；
3. **配速**：输入成绩并查看训练配速；
4. **课表**：选择模板、设置参数、周导航和训练日详情；
5. **统计**：基础训练统计；
6. **设置**：用户训练参数和数据说明；数据删除入口在 S4 与后端删除能力一起完成。

页面只负责收集输入、调用核心 API、渲染结果和调用 repository。页面内禁止自行计算配速、日期、完成率或训练规则。

### 7.1 数据访问

小程序页面通过 wapp 私有仓库内的 `TrainingDataService` 访问用户配置、课表和进度；该服务只依赖 `ScopedDataStore` 接口。首版由 `WechatCloudDataStore` 对接微信云开发。页面不得直接调用 `wx.cloud.database()`。未来切换 CloudBase 独立 SDK 或自建 API 时，只替换适配器，不改核心库和页面业务调用。首版可采用简单的本地缓存提升启动速度，但以云端记录为正式数据，不实现复杂离线队列和多设备冲突界面。

每次写入使用客户端操作 ID 保证幂等；更新时间以服务端为准。检测到版本不一致时先刷新云端数据并提示用户重试，不静默覆盖。

---

## 8. 实施路线图

### S0：内容和规则校准（2～4 天）

- 确认首版范围和核心 API；
- 修正课表周次与已知内容错误；
- 定稿内容、训练类型、PlanTemplate、PlanInstance 和 ProgressRecord schema；
- 确认配速算法的合法来源；
- 明确代码、原创内容和第三方材料的许可。

**验收：** 无待定 schema 字段；两套课表可完整表达；所有第三方数据有明确使用依据。

### S1：核心内容包（2～4 天）

- 建立 `packages/core`；
- 建立 Markdown 内容构建流程和内容索引；
- 迁移训练类型与两套结构化课表；
- 完成引用、图片、周次和 schema 校验；
- 对结构化课表进行逐格人工校对并留档。

**验收：** 23 篇内容均可通过 API 查询；课表与确认后的原文一致；非法引用和错误周次会使构建失败。

### S1.5：仓库与数据访问边界（1～2 天）

- 建立私有 `train-for-marathon-wapp` 仓库，通过 Git submodule 引用核心仓库；
- 在 wapp 内定义用户配置、课表和进度的统一读写接口；
- 提供内存适配器用于自动测试；
- 在 `apps/miniprogram` 内实现微信云开发适配器；
- 建立依赖边界检查，禁止核心库引用外层项目。

**验收：** 核心库、小程序和存储实现位于独立目录；替换测试适配器不影响业务调用；页面代码不直接访问云数据库。

### S2：核心业务能力（3～5 天）

- 实现配速计算；
- 实现模板实例化和日期绑定；
- 实现训练日调整与完成记录叠加；
- 实现基础统计；
- 建立核心单元测试和固定基准样例。

**验收：** 相同输入输出完全一致；核心包依赖扫描中不存在平台 API；日期跨月/跨年、配速边界、模板版本和统计样例全部通过。

### S3：微信小程序 MVP（5～8 天）

- 接入核心包并完成六个首版页面；
- 接入微信云开发身份；
- 建立三类最小用户数据与安全规则；
- 实现课表保存、打卡和统计展示；
- 完成加载、空状态、错误和重试体验。

**验收：** 两台真机使用两个测试账号分别完成“登录 → 阅读 → 计算配速 → 建立课表 → 查看当周 → 打卡 → 查看统计 → 重新进入恢复”闭环；账号间不能互读或修改数据；重复提交不产生重复记录。

### S4：发布准备（2～4 天，外部审核时间另计）

- 真机兼容、弱网、安全规则和异常输入测试；
- 补充隐私政策、用户协议、训练风险提示和数据删除流程；
- 在设置页接入云端数据删除入口及后端处理能力；
- 配置微信云开发正式环境、预算告警和备份；
- 完成小程序提审。

**验收：** 核心测试全部通过；权限隔离测试通过；仓库和构建产物无管理密钥；用户可以申请删除云端训练数据；发布清单完成。

> S0～S4 串行开发约 **14～25 个工作日（3～5 周）**。小程序认证、配速数据授权和平台审核属于外部前置时间，不计入开发工期。

---

## 9. 测试策略

### 9.1 核心测试

- 内容索引、内容引用和图片引用；
- 20 周与五周模板完整性；
- 配速算法基准样例与边界值；
- 比赛日期跨月、跨年和不同时区输入；
- 相同参数的确定性实例化；
- 训练日移动和替换；
- 完成次数、实际距离、完成率和连续周统计；
- schema 与模板版本兼容。

### 9.2 小程序测试

- 首次身份建立和再次进入；
- 内容、配速与课表结果和核心测试样例一致；
- 空数据、网络失败、写入失败和重试；
- 重复点击和重复请求幂等；
- 两个账号数据隔离；
- 云端数据删除流程；
- 常见手机尺寸和微信基础库版本。

---

## 10. 风险与合规

| 风险 | 首版处理方式 |
|---|---|
| VDOT 表版权 | 取得授权或采用可合法实现的计算方法；非盈利不视为授权 |
| 训练安全 | 展示风险和停止训练提示，不提供医疗诊断 |
| 核心被平台代码污染 | CI 扫描禁止依赖和平台全局对象 |
| 内容与结构化课表漂移 | 课表只保留一个结构化事实源，构建时生成展示 |
| 模板更新改变用户课表 | UserPlan 记录模板版本，不静默升级 |
| 用户数据泄漏 | 安全规则按用户隔离，使用双账号验证 |
| 微信云开发成本变化 | 正式环境、预算告警、限频和用量监控 |
| 许可证冲突 | 分别确认代码、原创内容和第三方材料的许可，并统一页面声明 |

仓库当前 `LICENSE` 是 CC0，但页面插件显示“all rights reserved”，二者存在冲突。在作者确认前不自动修改许可证，但发布前必须统一代码、原创内容和第三方材料的许可说明。

---

## 11. 已确认与实施前待确认

### 已确认

1. 首版先做微信小程序，不做网站；
2. 先完成平台无关的核心库；
3. 核心包含课程内容、配速、动态课表、完成记录模型和统计计算；
4. 微信小程序通过核心 API 完成功能，不重复实现业务规则；
5. 微信云开发只承担身份和最小用户数据，并由数据访问层隔离；
6. 用户账号与数据侧只做登录、打卡持久化和基础统计；课程阅读、配速计算和动态课表仍是首版核心功能。

### 实施前待确认

1. 小程序账号主体、认证状态和可用服务类目；
2. 20 周课表最后一行是否确认由第 8 周修正为第 1 周；
3. VDOT 数据授权或替代算法；
4. 代码、原创内容与第三方材料的许可证；
5. 配速成绩输入的合理上下限；
6. 用户云端数据删除后的保留期限。

---

## 12. 官方资料参考

- [CloudBase 身份认证](https://cloud.tencent.com/document/product/876/121347)
- [CloudBase 小程序端 SDK](https://cloud.tencent.com/document/product/876/19385)
- [CloudBase 安全规则](https://cloud.tencent.com/document/product/876/41802)
- [CloudBase 资源点价格](https://cloud.tencent.com/document/product/876/127357)
